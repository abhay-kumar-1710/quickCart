/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "m.media-amazon.com",
      "images.pexels.com",
      "i.pinimg.com",
      "cdni.iconscout.com",
    ],
  },
};

export default nextConfig;
